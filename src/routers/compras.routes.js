/* const { Router } = require("express");
const { getAllCompras, getCompraById, createCompra, updateCompra, deleteCompra } = require("../controllers/proveedor.controller");
const { verifyToken } = require("../utils/verifyToken");
const { buscarPermiso } = require("../validators/validations.validator");
const router = Router();

router.get("/getAllCompras", getAllCompras);

router.get("/getCompraById/:id", getCompraById);

router.post("/createCompra", createCompra);

router.put("/updateCompra/:id", updateCompra);

router.put("/statusCompra/:id", statusCompra);

router.delete("/deleteCompra/:id", deleteCompra);

module.exports = router;
 */